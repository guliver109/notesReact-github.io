import React from "react";

const NotFound = {
    paddingTop: "100px",
    textAlign: "center"
};

export default () => 

<div style = { NotFound } className = "NotFound">
    <h3> Sorry, page not found!</h3>
</div>